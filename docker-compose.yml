services:
  postgres:
    image: postgres:16-alpine
    container_name: bpms-postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: bpms
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  minio:
    image: minio/minio:latest
    container_name: bpms-minio
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: minioadmin
      MINIO_ROOT_PASSWORD: minioadmin
    ports:
      - "9000:9000"
      - "9001:9001"
    volumes:
      - minio_data:/data

  # api:
  #   build:
  #     context: .
  #     dockerfile: src/BPMS.Api/Dockerfile
  #   container_name: bpms-api
  #   ports:
  #     - "5000:8080"
  #   environment:
  #     ASPNETCORE_ENVIRONMENT: Development
  #     ConnectionStrings__DefaultConnection: "Host=postgres;Port=5432;Database=bpms;Username=postgres;Password=postgres"
  #     Jwt__SecretKey: "ThisIsASecretKeyForBpmsThatIsAtLeast32CharactersLong"
  #     Jwt__Issuer: "BPMS"
  #     Jwt__Audience: "BPMS"
  #     Jwt__ExpiryMinutes: "60"
  #   depends_on:
  #     - postgres
  #     - minio

volumes:
  postgres_data:
  minio_data: